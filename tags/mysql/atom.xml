<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: mysql | > kodama.gr_]]></title>
  <link href="http://www.kodama.gr/tags/mysql/atom.xml" rel="self"/>
  <link href="http://www.kodama.gr/"/>
  <updated>2013-10-13T14:34:25+03:00</updated>
  <id>http://www.kodama.gr/</id>
  <author>
    <name><![CDATA[Tasos Latsas]]></name>
    <email><![CDATA[tlatsas@gmx.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[change mysql root password]]></title>
    <link href="http://www.kodama.gr/2009/07/28/change-mysql-root-password/"/>
    <updated>2009-07-28T12:33:16+03:00</updated>
    <id>http://www.kodama.gr/2009/07/28/change-mysql-root-password</id>
    <content type="html"><![CDATA[<p>If you have forgotten your mysql root password all you need is the root password of the system to create a new one.</p>

<p>First, stop mysql and then start it in the background with the skip-grant-tables parameter:
<div class="highlight"><pre><code class="bash">mysqld_safe –skip-grant-tables &amp;amp;
</code></pre></div></p>

<p>Then login to mysql as root (you don’t need a pass anymore)
<div class="highlight"><pre><code class="bash">mysql -uroot
</code></pre></div></p>

<p>and type the following:
<div class="highlight"><pre><code class="mysql"><span class="n">mysql</span><span class="o">&amp;</span><span class="n">gt</span><span class="p">;</span> <span class="k">use</span> <span class="n">mysql</span><span class="p">;</span>
<span class="n">mysql</span><span class="o">&amp;</span><span class="n">gt</span><span class="p">;</span> <span class="k">update</span> <span class="n">user</span> <span class="kt">set</span> <span class="n">password</span><span class="o">=</span><span class="nf">PASSWORD</span><span class="p">(</span><span class="n">your</span><span class="o">-</span><span class="n">new</span><span class="o">-</span><span class="n">root</span><span class="o">-</span><span class="n">pass</span><span class="p">)</span> <span class="k">where</span> <span class="n">User</span><span class="o">=</span><span class="err">’</span><span class="n">root</span><span class="err">’</span><span class="p">;</span>
<span class="n">mysql</span><span class="o">&amp;</span><span class="n">gt</span><span class="p">;</span> <span class="n">flush</span> <span class="n">privileges</span><span class="p">;</span>
</code></pre></div></p>

<p>Don’t forget to kill the mysql process when you are done and restart it properly.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[set mysql encoding to utf8]]></title>
    <link href="http://www.kodama.gr/2009/07/28/set-mysql-encoding-to-utf8/"/>
    <updated>2009-07-28T00:00:00+03:00</updated>
    <id>http://www.kodama.gr/2009/07/28/set-mysql-encoding-to-utf8</id>
    <content type="html"><![CDATA[<p>This one troubled me for a long time and it also resulted in a corrupted database at some point, because i changed collation encoding to utf8 but the database encoding was latin. (so always make backups and make sure you don’t erase them :P )</p>

<p>Before doing the following make sure you backup any databases as this could possibly corrupt them. If you need to change the encoding of your database data, one possible solution <strong>(not tested)</strong> would be to use mysqldump to dump the database with the latin (or whatever) encoding. Then use the <a href="http://www.gnu.org/software/libiconv/documentation/libiconv/iconv.1.html">iconv</a> utility to change that encoding to utf8. After that change mysql encoding to utf8 (as follows) and then import the database with the new encoding.</p>

<p>Another solution <strong>(worked for me)</strong> is to let mysql “autodetect” the database encoding when you dump (mysqldump) and later import the databases.</p>

<p>So to set everything to utf8 encoding in mysql add the following lines to the [mysqld] section of my.cnf</p>

<p><div class="highlight"><pre><code class="text">init-connect = ‘SET NAMES utf8’
character-set-server = utf8
collation-server = utf8_general_ci
default-character-set = utf8
default-collation = utf8_general_ci
</code></pre></div></p>

<p>Also you need the following line under the [client] section of my.cnf
<div class="highlight"><pre><code class="text">default-character-set = utf8
</code></pre></div></p>

<p>then restart mysql.
You can verify the used encodings by loging in to mysql and running the following commands in cli:</p>

<p><div class="highlight"><pre><code class="text">mysql&amp;gt; show variables like ‘%server%’;
mysql&amp;gt; show variables like ‘%characters%’;
mysql&amp;gt; show variables like ‘%collation%’;
</code></pre></div></p>
]]></content>
  </entry>
  
</feed>
